import React from 'react';
import './NotFound.css';

const NotFound = () => {
    return (
        <div>
            <h4 className="text-danger text-center">The page you are looking for doesn't exists</h4>
        </div>
    );
};

export default NotFound;